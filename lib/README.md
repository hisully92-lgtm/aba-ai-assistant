# Project Overview

This is a unified AI engine built on a single route: POST /api/ai

All AI operations are handled through a single dispatcher: note, summary, timeline, report.

Full API documentation is in: `/docs/api/ai.md`